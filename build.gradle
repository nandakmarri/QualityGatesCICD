plugins {
    id 'java'
    id 'jacoco'
}

repositories {
    mavenCentral()
}

dependencies {
    implementation 'org.gosu-lang.gosu:gosu-core-api:1.17.4'
    implementation 'org.gosu-lang.gosu:gosu-core:1.17.4'
    implementation 'org.gosu-lang.gosu:gosu-test-api:1.17.4'
    testImplementation 'junit:junit:4.13.2'
    testImplementation 'org.hamcrest:hamcrest-core:1.3'
}

sourceSets {
    main {
        java {
            srcDirs = ['src/main/gosu']
        }
    }
    test {
        java {
            srcDirs = ['src/test/gosu', 'src/test/java']
        }
    }
}

test {
    useJUnit()
    testLogging {
        events "passed", "skipped", "failed"
        exceptionFormat "full"
    }
    reports {
        junitXml.required = true
        html.required = true
    }
    finalizedBy jacocoTestReport
}

jacoco {
    toolVersion = "0.8.8"
}

jacocoTestReport {
    dependsOn test
    reports {
        xml.required = true
        html.required = true
    }
}

jar {
    manifest {
        attributes(
            'Main-Class': 'main.gosu.Calculator'
        )
    }
}